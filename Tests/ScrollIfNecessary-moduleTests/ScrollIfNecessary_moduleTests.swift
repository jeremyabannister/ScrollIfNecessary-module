import XCTest
@testable import ScrollIfNecessary_module

final class ScrollIfNecessary_moduleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ScrollIfNecessary_module().text, "Hello, World!")
    }
}
